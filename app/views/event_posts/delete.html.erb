<p>※以下を削除してもいいですか？</p>
<table>
	<tr><th>Id</th>
		<td><%= @event_post.id %></td></tr>
	<tr><th>イベント名</th>
		<td><%= @event_post.title %></td></tr>
	<tr><th>イベント説明</th>
		<td><%= @event_post.content %></td></tr>
</table>
<%= form_tag controller: "event_posts", action: "delete" do %>
	<%= hidden_field_tag "id", {value: @event_post.id} %>
	<%= submit_tag "送信" %>
<% end %>
